using UnityEngine;

namespace Bounds.Infraestructura {

	public class ControlUI : MonoBehaviour {

		void Start() {/*
			TemaUI.SetGlobalColorBorde("BOTON", Colores.BORDE);
			TemaUI.SetGlobalColorRelleno("BOTON", Colores.RELLENO_BOTON);
			TemaUI.SetGlobalColorTexto("BOTON", Colores.LETRA);

			TemaUI.SetGlobalColorBorde("CARTEL", Colores.BORDE);
			TemaUI.SetGlobalColorRelleno("CARTEL", Colores.RELLENO_BOTON);
			TemaUI.SetGlobalColorTexto("CARTEL", Colores.LETRA);

			TemaUI.SetGlobalColorBorde("ENTRADA", Colores.BORDE);
			TemaUI.SetGlobalColorRelleno("ENTRADA", Colores.RELLENO_BOTON);
			TemaUI.SetGlobalColorTexto("ENTRADA", Colores.LETRA);

			TemaUI.SetGlobalColorBorde("FONDO_LOCAL", Colores.BORDE);
			TemaUI.SetGlobalColorRelleno("FONDO_LOCAL", Colores.RELLENO_CARTEL);

			TemaUI.SetGlobalColorRelleno("FONDO_GLOBAL", Colores.RELLENO_GLOBAL);*/
		}

	}

}